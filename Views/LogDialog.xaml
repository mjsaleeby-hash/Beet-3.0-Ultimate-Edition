<Window x:Class="BeetsBackup.Views.LogDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backup Log"
        Width="960" Height="460"
        WindowStartupLocation="CenterOwner"
        FontSize="14"
        FontFamily="Segoe UI Variable, Segoe UI"
        Background="#F3F3F3">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1A1A1A"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Backup Log"
                   FontSize="16" FontWeight="SemiBold" Foreground="#1A1A1A"/>

        <ListView Grid.Row="2" x:Name="LogList"
                  Background="White"
                  BorderBrush="#D0D0D0"
                  BorderThickness="1"
                  Foreground="#1A1A1A"
                  SelectionChanged="LogList_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time" Width="150" DisplayMemberBinding="{Binding TimestampDisplay}"/>
                    <GridViewColumn Header="Job" Width="140" DisplayMemberBinding="{Binding JobName}"/>
                    <GridViewColumn Header="Status" Width="160">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Running">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <ProgressBar Value="{Binding ProgressPercent}" Maximum="100"
                                                     Height="10" Width="130"
                                                     HorizontalAlignment="Left"
                                                     Foreground="#0078D4" Background="#D0D0D0"/>
                                        <TextBlock Text="{Binding Message}"
                                                   FontSize="11" Foreground="#F7630C"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <TextBlock Text="{Binding StatusDisplay}" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Running">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Complete">
                                                        <Setter Property="Foreground" Value="#107C10"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Failed">
                                                        <Setter Property="Foreground" Value="#C42B1C"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Scheduled">
                                                        <Setter Property="Foreground" Value="#0078D4"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Source" Width="130" DisplayMemberBinding="{Binding SourcePath}"/>
                    <GridViewColumn Header="Transfer Stats" Width="210" DisplayMemberBinding="{Binding StatsDisplay}"/>
                    <GridViewColumn Header="Details" Width="160" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Grid Grid.Row="4">
            <Button x:Name="RetryButton" Content="&#x1F504; Retry Failed Job"
                    Padding="16,8" Cursor="Hand"
                    HorizontalAlignment="Left"
                    Click="Retry_Click" IsEnabled="False"
                    ToolTip="Select a failed entry to retry">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="#F7630C" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="Bd" Property="Background" Value="#CCCCCC"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                                <Condition Property="IsEnabled" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Bd" Property="Background" Value="#E85A00"/>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Export CSV"
                        Background="#EBEBEB" Foreground="#1A1A1A"
                        BorderBrush="#D0D0D0" BorderThickness="1"
                        Padding="16,8" Margin="0,0,12,0" Cursor="Hand"
                        Click="ExportCsv_Click"/>
                <Button Content="Clear Log"
                        Background="#EBEBEB" Foreground="#1A1A1A"
                        BorderBrush="#D0D0D0" BorderThickness="1"
                        Padding="16,8" Margin="0,0,12,0" Cursor="Hand"
                        Click="ClearLog_Click"/>
                <Button Content="Close"
                        Background="#0078D4" Foreground="White"
                        BorderThickness="0"
                        Padding="16,8" Cursor="Hand"
                        Click="Close_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
